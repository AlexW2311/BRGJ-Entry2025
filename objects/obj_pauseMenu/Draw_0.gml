/// @description Insert description here
// You can write your code in this editor

if (global.isPaused)
{
    // Coordinates to center the sprite
    x = (room_width / 2);
    y = (room_height / 2);
    
    // Draw the pause menu sprite
    draw_sprite(spr_pauseMenu, 0, x, y);
    
    
}